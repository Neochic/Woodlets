{% if templates|length %}
<p>
    <label for="neochic-woodlets-page-template-input"><strong>Template</strong></label>
    <select class="wide" id="neochic-woodlets-page-template-input" name="neochic_woodlets_template">
        {% for key, label in templates %}
            <option value="{{ key }}"{% if key == template %} selected="selected"{% endif %}>{{ label }}</option>
        {% endfor %}
    </select>
</p>
{% endif %}
{# todo: add diable functionality
<p>
    <input type="hidden" name="neochic_woodlets_disabled" value="0" />
    <input type="checkbox" id="neochic-woodlets-disable-input" name="neochic_woodlets_disabled" value="1"{% if disabled %}checked="checked"{% endif %} />
    <label for="neochic-woodlets-disable-input">Disable Woodlets for this page.</label>
</p>
#}
