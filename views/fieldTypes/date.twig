{% extends '@woodlets/fieldType.twig' %}

{% block input %}
    <input class="widefat neochic-woodlets-date" type="date" id="{{ id }}" {{ macros.nameOrLink(customizer, name) }} value="{{ value }}" />
    <input class="widefat neochic-woodlets-date-jquery-ui" type="text" id="{{ id }}-jquery-ui" />
{% endblock %}
