<p>
    <label for="{{ id }}">
        <input id="{{ id }}" name="{{ name }}" type="checkbox" value="1"{% if value %} checked="checked"{% endif %} />
        {{ field.config.label ?: field.name }}
    </label>
</p>